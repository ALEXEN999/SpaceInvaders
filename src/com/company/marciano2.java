package com.company;

public class marciano2 {
    int x,y;
    int w,h;

    void pintar(){
        Canvas.fillRect(x+w,y+h,w,h);
        Canvas.fillRect(x+w*2,y+h,w,h);
        Canvas.fillRect(x+w*3,y+h,w,h);
        Canvas.fillRect(x+w*4,y+h,w,h);
        Canvas.fillRect(x+w*5,y+h,w,h);
        Canvas.fillRect(x+w,y+h*2,w,h);
        Canvas.fillRect(x+w*3,y+h*2,w,h);
        Canvas.fillRect(x+w*5,y+h*2,w,h);
        Canvas.fillRect(x+w,y+h*3,w,h);
        Canvas.fillRect(x+w*2,y+h*3,w,h);
        Canvas.fillRect(x+w*3,y+h*3,w,h);
        Canvas.fillRect(x+w*4,y+h*3,w,h);
        Canvas.fillRect(x+w*5,y+h*3,w,h);
        Canvas.fillRect(x+w*3,y+h*4,w,h);
        Canvas.fillRect(x+w,y+h*5,w,h);
        Canvas.fillRect(x+w*2,y+h*5,w,h);
        Canvas.fillRect(x+w*3,y+h*5,w,h);
        Canvas.fillRect(x+w*4,y+h*5,w,h);
        Canvas.fillRect(x+w*5,y+h*5,w,h);

    }
}
